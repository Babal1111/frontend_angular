"# frontend_angular" 







==css, inline, internal, import statments
== selectors in html-
-simple selectors
-combinator selectors
-psudo class selector
-psudo element selectors
-attribute selectors

--
inline css has more priority than internal
external/internal whichever is writtern first is given priority
-priority can be changed with !important


id has more priority than class and class has more priority than tag
-priority can be changed with !important

